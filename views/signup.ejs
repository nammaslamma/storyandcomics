<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up - Nimbus</title>
    <link rel="stylesheet" href="/css/styles.css"> <!-- Updated CSS path -->
</head>
<body>
    <header>
        <h1>Sign Up</h1>
    </header>

    <!-- Flash Messages -->
    <% if (error && error.length) { %> <!-- Improved condition -->
        <div class="flash-message error">
            <p><%= error %></p>
        </div>
    <% } %>

    <% if (success && success.length) { %> <!-- Improved condition -->
        <div class="flash-message success">
            <p><%= success %></p>
        </div>
    <% } %>

    <!-- Sign-Up Form -->
    <div class="form-container">
        <form action="/auth/signup" method="POST">
            <label for="username">Username:</label>
            <input type="text" name="username" id="username" required placeholder="Enter your username">

            <label for="email">Email:</label>
            <input type="email" name="email" id="email" required placeholder="Enter your email">

            <label for="password">Password:</label>
            <input type="password" name="password" id="password" required minlength="6" placeholder="Enter your password">

            <button type="submit">Sign Up</button>
        </form>
    </div>

    <style>
        body {
            font-family: 'Helvetica Neue', Arial, sans-serif;
            background-color: #f4f4f9;
            color: #333;
            display: flex;
            flex-direction: column;
            align-items: center;
            margin: 0;
            padding: 0;
        }

        header {
            padding: 2rem;
            width: 100%;
            background-color: #4a90e2;
            color: #fff;
        }

        h1 {
            margin: 0;
        }

        .flash-message {
            max-width: 90%;
            padding: 1rem;
            border-radius: 5px;
            margin: 1rem 0;
        }

        .flash-message.error {
            background-color: #ffdddd;
            color: #d8000c;
        }

        .flash-message.success {
            background-color: #ddffdd;
            color: #4f8a10;
        }

        .form-container {
            background: #ffffff;
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            max-width: 400px;
            width: 100%;
            margin-top: 2rem;
        }

        label {
            display: block;
            text-align: left;
            margin-bottom: 0.5rem;
            font-weight: bold;
        }

        input {
            width: calc(100% - 1.5rem);
            padding: 0.75rem;
            margin-bottom: 1rem;
            border: 2px solid #ddd;
            border-radius: 5px;
        }

        button {
            padding: 0.75rem 1.5rem;
            background-color: #4a90e2;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-weight: bold;
            transition: background-color 0.3s, transform 0.3s;
        }

        button:hover {
            background-color: #357ab8;
            transform: scale(1.05);
        }

        button:focus {
            outline: none;
            box-shadow: 0 0 3px #357ab8; /* Added focus state */
        }

        @media (max-width: 600px) {
            .form-container {
                width: 90%;
            }
        }
    </style>
</body>
</html>
